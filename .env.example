# Backend configuration
SECRET_KEY="supersecretkey_please_change_in_production"
# WARNING: Do not use * in production! Use * only for local/tunnel testing.
CORS_ORIGINS="http://localhost:5173,http://localhost:8000"

# Database
DATABASE_URL="sqlite:///./ner_oa.db"

# Frontend configuration (if any needed globally, can be prefixed with VITE_)
# Use /api when testing through the proxy or tunnel
# Use http://localhost:8000 when calling the backend directly
VITE_API_BASE_URL="/api"
